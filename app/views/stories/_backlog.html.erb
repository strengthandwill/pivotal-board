<div class="backlog panel panel-default col-md-<%= col_size %>">
  <div class="panel-heading"><strong><%= state %></strong> (<%= story_points %> sp)</div>
  <div class="panel-body">
    <% stories.each do |story| %>
      <%= render "stories/story", story: story %>
    <% end %>
  </div>
</div>