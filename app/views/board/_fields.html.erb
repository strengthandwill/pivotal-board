<span><strong><%= story.name %></strong></span>
<span class="story-id"><%= story.id %></span>
<% if(!story.estimate.nil? && story.estimate > 0) %>
  <span class="story-points"><%= story.estimate %> sp</span>
<% end %>
<br>
<% if(story.owners.length > 0) %>
  <% story.owners.each do |owner|%>
    <%= image_tag owner.image_path, size: size %>
  <% end %>
<% end %>