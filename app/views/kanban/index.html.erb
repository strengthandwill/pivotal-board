<% if @team.nil? %>
  <% provide :title, "Overall Kanban Board" %>
<% else %>
  <% provide :title, "#{@team.capitalize} Kanban Board" %>
<% end %>
<%= react_component 'Board', { team: @team, project_id: @project_id, backlog: @current, settings: @settings }, class: "board" %>